[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    spatial: tests that require PostGIS (skip if DATABASE_URL unavailable)
    integration: hits a live database / redis
filterwarnings =
    ignore::DeprecationWarning
